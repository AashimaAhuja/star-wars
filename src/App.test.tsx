import React from "react";
import { render } from "@testing-library/react";
import App from "./App";

// jest.mock("./Routes", () => <div data-testid="app"></div>);
test("renders App", () => {
  // const { getByTestId } = render(<App />);
  // expect(getByTestId("app")).toBeDefined();
});
